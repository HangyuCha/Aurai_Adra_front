# Frontend environment variables for local/dev
# Copy to `.env.local` and set your real values.

# Kakao JavaScript app key (from Kakao Developers console)
VITE_KAKAO_APP_KEY=your_kakao_js_key_here

# Optional: SRI hash for Kakao SDK; omit or set exact value to enable integrity check
# VITE_KAKAO_SDK_INTEGRITY=sha384-...

# Optional: Override backend Kakao exchange endpoints if your server uses custom paths
# VITE_KAKAO_CALLBACK_PATH=/login/oauth2/code/kakao
# VITE_KAKAO_AUTH_PATH=/auth/kakao

# Dev-only toggles (frontend middleware). Keep these empty/omitted when using a real backend.
# When set, frontend dev server will exchange code with Kakao directly (for UI testing only).
# DEV_KAKAO_EXCHANGE=1
# KAKAO_REST_KEY=your_kakao_rest_api_key
# KAKAO_CLIENT_SECRET=your_kakao_client_secret_if_any

# Optional dev-only mock for local signup (bypass backend CORS while prototyping)
# DEV_MOCK_REGISTER=1
